let nome = ''; // /* */ // String 
console.log(typeof nome); // ver o tipo
let flag = true; // Booleans
let variavel; // variavel undefined
let typenull = null
let lista = []
let bool = true;

console.log(bool);

bool = Boolean(nome)

console.log(bool);

// arrays

lista[0] = 'André';

lista.push(lista[0]);

console.log(lista);

// NaN - not a number

// cast string para number (int, float)
num = parseInt(nome);